<tr>
  <td><%= link_to post.titre, post %></td>
  <td><%= truncate(post.chapeau, length: 100) %></td>
  <td>
    <span class="<%= post.membres? ? 'has-text-success' : 'has-text-danger'%>">
      <%= t post.membres? %>
    </span>
  </td>
  <td><%= post.type_actu %></td>
  <td>
    <span class="tag is-medium is-light <%= post.style %>"><%= post.try(:workflow_state).try(:humanize) %></span> 
  </td>
  <td><%= time_ago_in_words post.updated_at %></td>
  <td><%= link_to 'Modifier', edit_post_path(post) %></td>
  <td><%= link_to '[X]', post, method: :delete, data: { confirm: 'Êtes-vous certain(e) ?' } %></td>
</tr>